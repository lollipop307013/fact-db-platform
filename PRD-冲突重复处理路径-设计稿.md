# 【AIGC】Gbot 事实库 — 冲突 / 重复事实处理路径 设计稿

> 文档版本：v0.1（草稿，2026-05-07）
> 状态：**设计中**，待评审后纳入 2 期 PRD 实施
> 关联文档：
> - `PRD-1期-Gbot事实库管理.md`（首期已落地：检测 + Tag 化展示 + 对比 Dialog）
> - `PRD-2期-Gbot事实库管理.md`（外部审核链路）

---

## 0. 文档定位

首期已经把"**检测**"和"**对比展示**"做完了：
- 提取审核区识别冲突 / 重复 → 红/黄 Tag 提示
- 点击 Tag → 对比 Dialog 看清差异（冲突看差异、重复看重叠）

但**之后怎么办**——这才是真正的业务闭环。本文档专门定义这个"处理路径"。

---

## 一、问题陈述

### 1. 现状

审核者看到冲突 / 重复 Tag 后，目前只能：
- 编辑新事实让它不再命中冲突（但旧事实仍在事实库里）
- 拒绝新事实（但放弃了新事实里可能有用的信息）
- 强行通过新事实入库（结果两条事实并存于库里互相打架）

**没有一个能处理"已有事实"的动作**——这是关键缺失。

### 2. 真实业务场景

| 场景 | 含义 | 当前缺失能力 |
|---|---|---|
| 新事实更准确，旧事实过时 | 版本更新后旧表述失效 | 一键"以新替旧"（旧事实下线） |
| 旧事实更准确，新事实是误判 | LLM 提取错了 | 一键拒绝新事实，旧事实保留 |
| 新旧事实描述不同子场景 | 两者其实不矛盾，只是细分 | 标记"已知非冲突"让它们共存 |
| 新旧事实需要合并补充 | 新事实带来新信息，旧事实有更全的语境 | 编辑改写新事实 / 把新内容并入旧事实 |
| 重复事实里新事实更全 | 新版本扩充了细节 | 用新替换旧 |
| 重复事实里新事实是冗余 | 旧事实已足够 | 拒绝新事实 |

---

## 二、处理路径设计

### 1. 冲突事实的 4 条处理路径

#### 1.1 以新替旧（最常用）

**触发**：审核者判断新事实更准确，旧事实需要废止。

**执行**：
1. 新事实状态 → 已审核（→ 入库后 → 已审核）
2. **旧事实**自动状态变更：已上线 → **已下线**（理由记录："被新事实 [新ID] 替换"）
3. 旧事实的 logs 写入"被替换"日志，引用新事实 ID
4. 新事实的 logs 写入"替换 [旧ID]"日志，引用旧事实
5. 新事实入库后通过常规审核流上线，自动取代旧事实

**前置校验**：
- 旧事实状态必须是"已上线"或"已下线"，不能在"待审核 / 已审核"中（这两种状态意味着尚未发布给玩家）
- 操作者必须有权限修改旧事实（首期不做权限控制，所有人可操作）

**回滚**：
- 操作日志可追溯，必要时手动恢复（旧事实手动从已下线 → 已上线，新事实手动下线）

#### 1.2 以旧为准（拒绝新事实）

**触发**：审核者判断旧事实正确，新事实是误判。

**执行**：
1. 新事实状态 → 已拒绝（缓冲池态）
2. 旧事实**不变**
3. 新事实的 logs 写入"被拒绝原因：与 [旧ID] 冲突，以旧事实为准"

#### 1.3 编辑改写

**触发**：审核者认为新旧不是真正冲突，只是表述差异；通过改写新事实让它兼容旧事实，或合并双方信息。

**执行**：
1. 打开编辑抽屉修改新事实内容
2. 保存时自动重新校验：
   - 如果改后**不再命中冲突** → 进入正常审核流
   - 如果改后**仍命中冲突** → 提示用户继续选择 1.1 / 1.2 / 1.4

#### 1.4 标记"已知非冲突"共存

**触发**：审核者明确判断双方描述的是不同子场景（如新事实是"A 点"，旧事实是"B 点"，但 LLM 把它们误判为冲突）。

**执行**：
1. 新事实状态 → 已审核
2. **建立"已澄清"关系**：两条事实互相引用，标记 `relation: "non-conflicting"`
3. 后续 LLM 再次检测到这对组合时，自动跳过冲突告警
4. 双方事实的 logs 写入"标记为非冲突，澄清原因：xxx"
5. 用户必须填写澄清原因（必填，避免被滥用）

**安全机制**：
- 澄清关系可被撤销（恢复为待校验状态）
- 列表里两条事实都会显示"已澄清非冲突"小图标，可点击看原因

---

### 2. 重复事实的 3 条处理路径

#### 2.1 拒绝新事实（最常用）

**触发**：新事实信息已被旧事实完全覆盖，无新增价值。

**执行**：
1. 新事实状态 → 已拒绝
2. 旧事实**不变**
3. logs 写入"被拒绝：与 [旧ID] 重复"

#### 2.2 以新替旧

**触发**：新事实表述更全面 / 更准确，旧事实可以淘汰。

**执行**：
1. 新事实状态 → 已审核（→ 入库后通过审核流上线）
2. 旧事实自动 已上线 → 已下线
3. 双方互相引用 logs

**前置校验**：与冲突 1.1 一致

#### 2.3 合并入库

**触发**：双方各有补充，需要取并集。

**执行**：
1. 打开编辑抽屉，**预填"合并草稿"**：
   - 内容：用 LLM 合并旧 + 新（生产环境实现；本期 mock 为：拼接新内容到旧内容末尾）
   - 关联实体 / 事件：取双方并集
   - 时间：取新事实的时间（更新更可靠）
2. 用户确认 / 修改后保存
3. 保存等同于"以新替旧"：合并后新事实通过审核入库，旧事实自动下线

---

## 三、UI 交互设计

### 1. 对比 Dialog 底部加"处理动作区"

在 CompareDialog 的关闭按钮旁加一组主动作：

#### 1.1 冲突 Dialog

```
┌─ 冲突事实对比 ──────────────────────────────────┐
│ ⚠ 冲突 ID:37957                                │
│ 关键差异点：                                     │
│  · 激活方式：自动触发 ↔ 手动激活                  │
│  · 触发条件：接触敌人 ↔ 投掷激活                  │
│ ┌─ 已有事实 ─────────┬─ 新事实 ──────────────┐ │
│ │ ...自动触发...    │ ...手动激活...        │ │
│ └───────────────────┴───────────────────────┘ │
│                                                │
│ ─────────────────────────────────────────────  │
│ 处理动作：                                      │
│ [✓ 以新替旧]  [✗ 以旧为准]  [✏ 编辑新事实]      │
│ [🤝 标记非冲突]                       [关闭]    │
└────────────────────────────────────────────────┘
```

#### 1.2 重复 Dialog

```
┌─ 重复事实对比 ──────────────────────────────────┐
│ ≈ 重复 ID:42010 · 重叠率 86%                    │
│ 主要重叠表述：                                   │
│  · 地点：日落之城 A 点                           │
│  · 技能：剃刀藤蔓 + 弧光玫瑰                     │
│ ┌─ 已有事实 ─────────┬─ 新事实 ──────────────┐ │
│ │ ...                │ ...                  │ │
│ └───────────────────┴───────────────────────┘ │
│                                                │
│ ─────────────────────────────────────────────  │
│ 处理动作：                                      │
│ [✗ 拒绝新事实]  [✓ 以新替旧]  [🔀 合并入库]     │
│                                       [关闭]    │
└────────────────────────────────────────────────┘
```

### 2. 二次确认 Dialog

涉及"以新替旧"、"合并入库"这种**有跨记录联动**的操作，必须二次确认：

```
确认替换？

新事实将通过审核入库；
旧事实 [ID:37957] 将自动下线（状态：已上线 → 已下线）。

旧事实下线后将不再用于 AI 问答检索。
此操作可通过操作日志追溯并手动回滚。

[确认替换]  [取消]
```

### 3. 标记"非冲突"的填表 Dialog

```
标记为非冲突

请填写澄清原因（必填）：
┌────────────────────────────────────────┐
│ 例如：新事实描述 A 点战术，旧事实是 B 点， │
│ LLM 误判为冲突，实为不同子场景。          │
└────────────────────────────────────────┘

填写后两条事实将共存，后续不再触发冲突告警。

[提交]  [取消]
```

### 4. 列表展示——已澄清非冲突标记

事实库列表的"矛盾事实"列：
- 原本：`⚠ 红色 Tag [ID:37957] xxx`
- 标记后：`⚠ 灰色 Tag [ID:37957] · 🛡 已澄清` ， hover 看澄清原因

---

## 四、数据模型扩展

### 1. 事实表新增字段

```ts
interface Fact {
  // 原有字段...

  /** 与本事实的"已澄清非冲突"关系（值是另一条事实的 ID） */
  clarifiedNonConflictWith?: string[];
  /** 替换关系：本事实是被哪条事实替换下线的 */
  replacedBy?: string;
  /** 替换关系：本事实替换了哪些旧事实 */
  replaces?: string[];
}
```

### 2. 操作日志新增 action

```ts
type LogAction =
  | "创建-手动" | "创建-导入" | "创建-提取" | "编辑" | "状态变更"
  | "回退" | "同步" | "删除"
  | "替换"          // 本事实替换了旧事实，detail: "替换 [旧ID]"
  | "被替换"        // 本事实被新事实替换下线，detail: "被 [新ID] 替换"
  | "标记非冲突"    // detail: "与 [对方ID] 标记为已澄清，原因：xxx"
  | "撤销非冲突标记";
```

### 3. 缓冲池条目处理记录

```ts
interface ExtractedFact {
  // 原有字段...

  /** 处理动作（仅当条目走过冲突/重复处理路径时记录） */
  resolution?: {
    type: "replace" | "reject-by-old" | "edit" | "mark-non-conflict" | "merge";
    targetFactId: string;          // 关联的旧事实 ID
    reason?: string;                // 澄清原因（仅 mark-non-conflict）
    operator: string;
    time: string;
  };
}
```

---

## 五、流程图（文字描述）

### 1. 冲突处理决策树

```
新事实 ──检测到冲突──→ 审核者打开对比 Dialog
                            │
        ┌───────────────────┼─────────────────┬─────────────────┐
        ↓                   ↓                 ↓                 ↓
    [以新替旧]          [以旧为准]         [编辑改写]       [标记非冲突]
        │                   │                 │                 │
        ↓                   ↓                 ↓                 ↓
  二次确认                 直接生效         打开抽屉           填澄清原因
        │                   │                 │                 │
        ↓                   ↓                 ↓                 ↓
  新事实入审核              新事实拒绝     保存重校验          双方共存
  旧事实下线              （缓冲池态）        │                 │
        │                   │                 ↓                 ↓
        ↓                   ↓             仍冲突？再选择      logs 写入
  双方 logs 互引       logs 写入原因         │
                                            ↓
                                         不冲突？正常审核
```

### 2. 重复处理决策树

```
新事实 ──检测到重复──→ 审核者打开对比 Dialog
                            │
        ┌───────────────────┼─────────────────┐
        ↓                   ↓                 ↓
    [拒绝新事实]        [以新替旧]         [合并入库]
        │                   │                 │
        ↓                   ↓                 ↓
   直接拒绝             二次确认           预填合并草稿
        │                   │                 │
        ↓                   ↓                 ↓
   logs 写入原因      新事实入审核         打开抽屉
                       旧事实下线              │
                            │                  ↓
                            ↓               用户调整内容
                      logs 互引                │
                                              ↓
                                      保存 = 等同以新替旧
```

---

## 六、安全规则

### 1. 跨记录联动的保护

涉及"修改其它已上线事实状态"的动作，必须满足：
- **二次确认**：弹 Dialog 明确说明"X 条已上线事实将下线"
- **不可批量绕过**：批量入库时，如果其中某条带 resolution.replace，**必须逐条二次确认**，不能一键通过
- **完整 logs**：双方事实的 logs 必须互相引用，留存原因和操作人
- **可回滚**：被自动下线的事实保留完整数据，操作日志中提供"恢复上线"快捷按钮（手动操作）

### 2. 权限边界（首期内 + 可扩展）

- 首期：所有审核者均可执行所有动作
- 后续可扩展：
  - "以新替旧"和"合并入库" → 仅资深审核者可操作
  - 跨语言版本的事实替换 → 必须确认所有语言版本都已审核

### 3. 防止无限循环

- A 标记 B 为"非冲突"后，再次提取出与 A 类似的新事实，仍可能与 A 冲突 —— 不应该自动免检
- "已澄清非冲突"只在 **A↔B 这一对** 之间生效，不传递

---

## 七、实施分期

### 阶段 1（与外部审核同期，2 期上半）：基础处理路径

P0 范围：
- 冲突 1.1（以新替旧）+ 1.2（以旧为准）+ 1.3（编辑改写）
- 重复 2.1（拒绝）+ 2.2（以新替旧）
- 数据模型扩展（replacedBy / replaces / 新 LogAction）
- 二次确认 Dialog
- 操作日志互引

### 阶段 2（2 期下半）：合并与澄清

P1 范围：
- 冲突 1.4（标记非冲突）
- 重复 2.3（合并入库，含 LLM 合并草稿生成）
- 列表"已澄清非冲突"小图标
- 列表"被替换 / 替换" 关系链可视化

### 阶段 3（3 期）：批量与权限

P2 范围：
- 批量冲突 / 重复处理（多条选中后批量"拒绝"或"以新替旧"）
- 操作权限控制
- 替换关系链全景视图（看清一条事实被替换了多少代）

---

## 八、待评审的开放问题

1. **"以新替旧"的旧事实状态走向**：自动下线，还是允许审核者选择"下线 / 删除"？
   - 倾向：**自动下线**，删除太重、不可恢复
2. **"合并入库"的 LLM 草稿是否必填**：用户可以全删重写吗？
   - 倾向：**预填，可全部改写**
3. **冲突澄清原因的格式**：自由文本 vs 固定枚举（不同子场景 / LLM 误判 / 历史版本差异 / 其它）？
   - 倾向：**枚举 + 自由文本补充**，便于后续统计 LLM 误判率
4. **批次内多条冲突 / 重复一起处理**的入口位置？
5. **"被替换"的旧事实，AI 问答检索是否完全屏蔽**，还是降权处理？
   - 倾向：**降权但不屏蔽**（万一新事实有问题需要回滚）

---

## 九、版本记录

| 版本 | 日期 | 变更 | 作者 |
|---|---|---|---|
| v0.1 | 2026-05-07 | 设计稿初版 | yzhinan |
