# 需求细节变更记录

> 用途：记录后续每次需求变更的原始描述、实现方案、影响范围与验收结果，便于回溯与对齐。

## 记录规范

- 每次需求改动新增一条记录（不要覆盖历史）。
- 先写**需求原文/目标**，再写**实现细节**与**验收点**。
- 若有争议点，补充**待确认项**。

---

## 变更记录模板

### [YYYY-MM-DD HH:mm] 标题（模块）
- **需求原文**：
- **目标**：
- **实现细节**：
  - 
- **影响文件**：
  - `路径/文件名`
- **验收点**：
  - 
- **结果**：
  - [ ] 完成
  - [ ] 部分完成
  - [ ] 待处理
- **待确认项**（可选）：
  - 

---

## 当前会话起始记录

### [2026-08-12 00:00] 建立需求细节记录机制（通用）
- **需求原文**：后续的改动额外建一个 md 记录下来，用于整理需求细节描述。
- **目标**：建立统一文档，承接后续每次需求改动的结构化记录。
- **实现细节**：
  - 新建 `需求细节变更记录.md`。
  - 约定固定字段：需求原文、目标、实现细节、影响文件、验收点、结果、待确认项。
- **影响文件**：
  - `需求细节变更记录.md`
- **验收点**：
  - 文档可直接追加新记录。
  - 模板字段完整，便于后续复盘。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理

---

### [2026-08-12 18:30] 截止当前细节改动汇总（知识库/质检优化/事件管理/事实提取）
- **需求原文**：整理下目前为止做的细节改动。
- **目标**：形成可回溯的阶段性变更清单，便于需求复盘与继续迭代。
- **实现细节**：
  - 导航与模块联动：还原一级/二级侧边栏结构，打通知识库与质检优化切换；知识库默认回到事实库页面，质检优化进入质检任务。
  - 页面路由与接入：内容库下接入 `事实提取`、`问题回复`、`内容审核`、`错误表述检测` 等页面，`qa-check` 对接现有审核任务视图。
  - 主视觉优化：主区域统一白色卡片、标题与工具栏样式优化、统计区与筛选区层次重排。
  - 事实提取：移除模型号选择，仅保留提取模式；文件提取新增“下载模板/刷新模板”，并落地 `fact_extract_template.csv` 静态模板。
  - 事实管理：对齐线上布局（左侧分类树 + 右侧事实表），分类按钮显示叶子节点统计。
  - 事件管理层级：建立一级视图（默认/列表/树状）+ 二级类型（限期事件/周期事件/未分类），并将一级切换并入筛选行。
  - 事件甘特能力：支持今天竖线、中部滚轮缩放、左右区域滚动、动态日期刻度与碰撞隐藏、分页与每页条数（5/10/20/50）、超 10 条时拖拽高度。
  - 事件文案与规则：统一命名为“限期事件/周期事件/未分类/限时周期”；限时周期持续天数由起止日期自动计算（只读展示）。
  - 审核字段收敛：实体/事实/事件页逐步移除审核标签与筛选，审核能力集中到审核工作台。
  - 稳定性修复：修复 `audit is not defined`、滚轮空指针、对话框无效 `bodyStyle` 透传告警等导致白屏/报错的问题。
- **影响文件**：
  - `src/App.tsx`
  - `src/cases/fact-db/Page.tsx`
  - `src/cases/fact-db/style.css`
  - `src/cases/fact-db/components/EventTab.tsx`
  - `src/cases/fact-db/components/EventDialog.tsx`
  - `src/cases/fact-db/components/ExtractTab.tsx`
  - `src/cases/fact-db/components/FactTab.tsx`
  - `src/cases/fact-db/components/EntityDialog.tsx`
  - `src/cases/fact-db/components/FactDialog.tsx`
  - `src/cases/fact-db/components/CategoryDialog.tsx`
  - `src/cases/fact-db/mock.ts`
  - `src/cases/fact-db/types.ts`
  - `public/fact_extract_template.csv`
- **验收点**：
  - 可从导航在知识库与质检优化之间稳定切换。
  - 事件管理关键交互（视图层级、甘特缩放、分页、命名）符合当前需求口径。
  - 事实提取可下载并刷新模板。
  - 当前页面无阻断性编译错误与白屏问题。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理

---

### [2026-08-12 19:20] 事件页白屏与周期视图修复 + 三Sheet导入模板（事实提取）
- **需求原文**：
  - 修复页面展示问题，周期事件展示错误。
  - 文件导入“刷新模板”需升级为增加模板 Sheet，并按用户填写的 Sheet 定向处理。
  - 模板应为 3 个 Sheet，且页面提供导入说明（信息提示或下载前弹窗）。
- **目标**：恢复事件页可用性，落地可执行的三 Sheet 导入链路并提升导入指引可读性。
- **实现细节**：
  - 事件页：移除周期图例中残留的 `statusMap` 引用；为滚轮监听补充挂载态保护，避免空节点调用 `getBoundingClientRect`。
  - 事实提取：`handleFileSelect` 升级为 `xlsx` 多 Sheet 解析（`实体` / `事件` / `知识资讯`），并为每个片段记录 `sheetType` 与 `rowData`。
  - 定向处理：提取入库阶段按 `sheetType` 映射实体/事件/知识候选（实体表优先实体、事件表优先事件、知识表走知识文本）。
  - 模板下载：模板文件升级为 `fact_extract_template.xlsx`（3 Sheet），按钮改为“刷新模板结构/下载模板”。
  - 导入说明：下载模板前增加说明弹窗，明确三 Sheet 的填写与处理规则。
- **影响文件**：
  - `src/cases/fact-db/components/EventTab.tsx`
  - `src/cases/fact-db/components/ExtractTab.tsx`
  - `public/fact_extract_template.xlsx`
  - `package.json`（新增 `xlsx` 依赖）
- **验收点**：
  - 事件页不再因周期视图切换而白屏。
  - 下载模板后可见 3 个 Sheet：`实体`、`事件`、`知识资讯`。
  - 上传三 Sheet 模板后，系统可按不同 Sheet 产生定向候选。
  - 下载模板前可看到导入说明弹窗。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理

---

### [2026-08-12 20:05] 限期视图布局重排（标题与筛选分页位置）
- **需求原文**：
  - 这里改成时间线，不需要带数量。
  - 选页和筛选都放在下方卡片列表上面，甘特图之下。
- **目标**：让限期视图结构与视觉层级更贴近目标稿，减少甘特区干扰项。
- **实现细节**：
  - 甘特区标题由“限期事件对比（N）”改为“时间线”（不显示数量）。
  - 将状态筛选、每页条数、分页信息与翻页按钮整体从甘特图头部移至甘特图下方。
  - 工具栏插入到卡片时间线列表上方，并补充 `.span-compare-tools--below` 间距样式。
- **影响文件**：
  - `src/cases/fact-db/components/EventTab.tsx`
  - `src/cases/fact-db/style.css`
- **验收点**：
  - 甘特图标题仅显示“时间线”。
  - 筛选/分页控件出现在甘特图下、卡片列表上方。
  - 页面无新增编译或 lint 报错。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理

---

### [2026-08-12 20:20] 限时文案统一 + 下方时间线列表联动分页筛选
- **需求原文**：
  - 把限期的表述全改成限时。
  - 每页数量、筛选和切换页码，也影响下面的列表。
- **目标**：统一术语口径，并确保甘特区控制项与下方卡片时间线使用同一数据集。
- **实现细节**：
  - 将事件页与编辑弹窗中的“限期”文案统一替换为“限时”。
  - 下方时间线分组改为基于 `spanEventsPaged`，不再使用仅筛选未分页数据。
  - 列表空态判断改为 `spanEventsPaged.length === 0`，保证翻页/每页条数变化能即时反映在列表区域。
- **影响文件**：
  - `src/cases/fact-db/components/EventTab.tsx`
  - `src/cases/fact-db/components/EventDialog.tsx`
- **验收点**：
  - 页面中“限期事件”统一为“限时事件”。
  - 调整筛选、每页条数或页码时，下方卡片时间线同步变化。
  - 类型检查与 lint 均无新增错误。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理

---

### [2026-08-12 21:00] 审核任务列表与左右屏审核改造（内容审核）
- **需求原文**：
  - 审核任务列表字段对齐截图，增加“参与审核人”，点击 RTX 标签展示自动统计的审核数量及事实/实体/事件类型分布；移除无意义的查看日志入口。
  - 增加“有待审条目 / 已全部处理 / 所有任务”筛选。
  - 进入任务后左侧展示条目 ID、对象 ID、变更、优先级、审核结果；右侧按数据类型分区展示，可收起展开。
  - 新增无冲突仅展示新数据；新增冲突/重复、覆盖更新展示字段级新旧对比，淡化相同字段、强调差异；通过编辑模式修改待审新字段后再确定审核结论。
- **目标**：降低审核人员识别任务进度、处理人和字段差异的成本，并将审核过程稳定在左右屏工作区内完成。
- **实现细节**：
  - 任务表保留任务 ID、标题/编号、来源类型、内容类型、待审/总数、任务进度、创建时间、操作等字段，并前置“参与审核人”列。
  - 以条目的最终审核人自动汇总每个任务的审核数量与内容类型分布；RTX 标签显示数量，点击浮层显示“某人已审核 N 条 / 事实、实体、事件数量”。
  - 移除任务列表和详情页的日志查看入口；审核记录仍保留为内部统计与审计数据。
  - 新增三类任务状态筛选：有待审条目、已全部处理、所有任务。
  - 左侧列表统一为紧凑字段栅格；右侧按事实、实体、事件、时间、来源、冲突/重复、关联、原文、QA 原始信息组织可折叠分区。
  - 新增无冲突使用单列新数据展示；冲突/重复和覆盖更新采用字段级对比，字符差异高亮、相同字段弱化；编辑在右侧内联完成，无弹窗。
  - 补充审核任务标题、优先级、对象 ID、冲突/重复及 QA 原始信息 mock，以覆盖主要审核场景。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review-mock.ts`
  - `src/cases/fact-db/review-types.ts`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 可切换三类任务状态筛选，已全部处理任务不再混入有待审条目。
  - 点击 `yzhinan（1）` 等 RTX 标签可展示已审核总数和类型分布。
  - 左侧条目列表字段固定为条目 ID、对象 ID、变更、优先级、审核结果。
  - 覆盖和重复场景展示新旧差异；新增无冲突不展示新旧对比；右侧编辑保存后字段即时更新。
  - 审核过程无详情弹窗，浏览器控制台无运行时错误。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理
- **验证说明**：
  - 已通过本地浏览器完成任务筛选、RTX 浮层、左右屏详情、重复场景编辑保存、无冲突新增展示验证。
  - 审核相关文件 lint 通过，且未出现在项目 TypeScript 构建错误中；全量构建仍存在本次改动之外的历史错误。

---

### [2026-08-12 21:20] 审核分区聚焦与任务编号极简化（内容审核）
- **需求原文**：
  - 自动展开存在字段差异的区块；无变更、无新增或无内容区块默认收起。
  - 任务 ID 改为从创建起分配的唯一顺序号（`#1`、`#2`），删除后不复用；移除任务进度列。
  - 优化内容审核任务列表视觉，采用更极简、高级的风格。
- **目标**：减少审核首屏噪音，明确稳定任务编号，并提升任务列表的数据密度与可读性。
- **实现细节**：
  - 字段区块按真实新旧差异判断默认展开：存在差异或待入库内容时展开；无变更/无新增内容时收起。区块头部显示“差异 N 处”“待入库 N 处”“无变更”或“无新增内容”。
  - 来源、关联、原文、QA 原始信息、冲突/重复等辅助信息保持默认收起，审核人可按需展开。
  - 新增 `ReviewTask.sequenceNo` 与 `displayId`；原型使用稳定的任务顺序号映射模拟服务端持久化序列，内部批次键、日志关联和跨任务联动继续使用原始 `taskId`。
  - 列表任务号改为 `#N`，详情头、来源信息和跨任务提示均统一使用该展示编号；移除任务进度列与重复的内部批次号展示。
  - 任务表改为轻量无纵向网格样式，压缩行距与标签层级，优化任务号、待审计数、审核人标签、时间与操作按钮的排版。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review-mock.ts`
  - `src/cases/fact-db/review-types.ts`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 有差异的事实/时间字段区块默认展开；来源、关联、原文等无直接待审内容的区块默认收起。
  - 新增无冲突任务仅展开含待入库内容的字段区块。
  - 任务列表只显示稳定的 `#N` 任务号，且不再包含“任务进度”列。
  - 列表可搜索 `#N`，任务详情和跨任务提示的编号口径一致。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理
- **验证说明**：
  - 已通过本地浏览器确认任务列表显示 `#10`、`#9` 等顺序号且无任务进度列。
  - 已验证覆盖变更自动展开事实正文与时间信息；新增无冲突任务仅展开待入库内容，辅助区块保持收起。
  - 审核相关 lint 通过，且本次文件未出现在 TypeScript 构建错误中。

---

### [2026-08-12 21:45] 审核任务进度合并与全量排错
- **需求原文**：
  - 保留任务编号。
  - 将内容类型和待审/总数合并为“类型及审核进度”，标签样式如 `事实1/25`。
  - 移除任务列表上方的统计区；顺便排错。
- **目标**：进一步压缩审核任务列表的重复信息，并清理当前原型中的编译和运行时错误。
- **实现细节**：
  - 保留稳定任务展示编号 `#N`，不恢复内部批次 ID。
  - 新增 `ReviewTask.typeProgress`，按事实、实体、事件分别统计待审/总数；列表将“内容类型”和“待审/总数”合并为“类型及审核进度”，如 `事实1/25`。
  - 移除任务列表顶部“待审核数据量、待审核任务数、已通过、已拒绝、任务总数”统计区，并删除对应样式。
  - 调整任务列表标签视觉，使类型进度、来源、审核人保持轻量化但可辨识。
  - 修复 TypeScript 错误：导航 Lucide 图标类型、分类/实体/事件/事实表单的过期 TDesign 属性、事件表单类型别名、页面菜单重复键，以及事实/实体编辑弹窗和列表间的保存回写契约。
  - 新增本地 `favicon.svg` 并在入口声明，消除浏览器请求缺失 `favicon.ico` 的 404。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review-types.ts`
  - `src/cases/fact-db/review-mock.ts`
  - `src/cases/fact-db/review.css`
  - `src/App.tsx`
  - `src/cases/fact-db/types.ts`
  - `src/cases/fact-db/Page.tsx`
  - `src/cases/fact-db/components/{CategoryDialog,EntityDialog,EntityTab,EventDialog,FactDialog,FactTab}.tsx`
  - `public/favicon.svg`
  - `index.html`
  - `需求细节变更记录.md`
- **验收点**：
  - 任务编号仍显示为 `#N`。
  - 任务列表不展示顶部统计和独立“待审/总数”列。
  - “类型及审核进度”按类型展示 `事实待审/总数`、`实体待审/总数`、`事件待审/总数`。
  - `npm run build` 全量通过，浏览器控制台没有错误。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理
- **验证说明**：
  - 已通过本地浏览器确认顶部统计已移除，列表显示 `#10` 等稳定任务编号，以及 `实体1/1`、`事实1/2` 形式的合并进度标签。
  - 已验证事实编辑弹窗可正常打开，浏览器控制台无错误。
  - 已执行完整 `npm run build`，构建通过。

---

### [2026-08-12 22:10] 审核详情工作区、位置记忆与双视图（内容审核）
- **需求原文**：
  - 在任务详情中展示待审核数据列表；任务区域支持搜索、列头筛选、分页和批量操作。
  - 审核操作固定在底部，按变更类型显示“通过并新增 / 通过并覆盖 / 通过并删除”，并保留低饱和红色的拒绝与“编辑新数据”；暂不提供另存为。
  - 审核区域固定页面占比，字段扩展内容在区域内部滚动查看。
  - 离开后再次进入同一任务，应恢复当前审核数据、分页设置、页码等历史位置。
  - 增加简化/原始视图切换；原始视图保留新旧版本的完整配置字段；明确展示被对比对象，避免重复 ID；移除右上角范围标签。
- **目标**：将审核流程收敛为稳定的任务内工作台，降低长内容滚动、跨任务误操作和重新进入任务后定位成本。
- **实现细节**：
  - 详情左栏明确为“审核数据列表”，保留待审核、已通过、已拒绝三个顶部状态分组；移除重复的审核结果列，新增类型列。
  - 搜索支持条目 ID、对象 ID、标题；类型、变更、优先级通过对应列头的下拉多选筛选。筛选后会重置页码，并裁剪不在当前筛选结果内的勾选项。
  - 左栏增加每页 5 / 10 / 20 / 50 的分页。全选和批量操作仅作用于“当前任务 + 当前待审核状态 + 当前筛选结果”中的显式勾选条目；只有多选时展示“批量通过 / 批量拒绝”。
  - 取消同事实跨任务的自动联动审核，关联任务仅提示，防止未查看的其它候选版本被批量处理。
  - 以 `localStorage` 按任务记忆状态分组、当前条目、待审版本、筛选条件、页大小、页码和左栏滚动位置；重新进入任务后恢复。简化/原始视图偏好也会持续保存。
  - 审核工作区使用固定高度左右栏，左侧列表与右侧详情各自滚动，右侧操作栏固定在底部。
  - 简化视图延续字段差异与折叠区块；原始视图以“线上生效版本 / 新数据（待审）”双列完整呈现配置字段及来源快照。
  - 审核详情标题使用唯一对象标识（如 `事实 #10042 · 标题`），不再额外重复显示对象 ID；右侧“字段发生变化”旁加入视图切换。
  - 通过按钮按变更类型自动切换为通过并新增、通过并覆盖或通过并删除；移除另存为实现与遗留样式。内容审核页不再展示右上角项目/渠道范围胶囊标签。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review-types.ts`
  - `src/cases/fact-db/review.css`
  - `src/cases/fact-db/Page.tsx`
  - `需求细节变更记录.md`
- **验收点**：
  - 待审核数据列表可搜索、按类型/变更/优先级筛选、分页，并且不显示审核结果列。
  - 批量按钮仅在多选时显示，且只处理当前筛选结果；不同变更类型单条审核显示正确的“通过并…”文案。
  - 长字段展开时不撑开页面工作区，底部操作始终可见。
  - 返回任务列表再进入后恢复对应任务中的查看条目、页码/页大小和视图偏好。
  - 原始视图双列显示新旧配置字段，审核头仅显示一个对象 ID，并可明确识别对比对象。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理
- **验证说明**：
  - 已通过本地浏览器验证：待审核列表、列头筛选弹层、多选后批量按钮、固定底部操作栏、简化/原始双视图、离开再进入恢复原始视图与当前条目。
  - 已验证新增、覆盖、删除分别显示“通过并新增 / 通过并覆盖 / 通过并删除”，删除条目不展示编辑新数据。
  - 浏览器控制台无错误，相关文件 lint 通过，`npm run build` 全量通过。

---

### [2026-08-13 11:10] 审核列表空间优化与编辑窗口字段修复
- **需求原文**：
  - 全选放在条目 ID 左侧，不再单独占一行；待审核、已通过、已拒绝收紧，避免压榨标注列表。
  - 修复事实、事件、实体等编辑窗口的字段展示；事件遵循新的事件设计。
  - 审核列表字段放宽，展示完整标题；审核工作台底部减少空白，优先保障列表空间。
- **目标**：提升任务内审核信息密度和长标题可读性，并保证各数据类型的编辑窗口字段完整、预填稳定、可保存。
- **实现细节**：
  - 将全选框移动至待审核列表表头首列、紧邻“条目 ID”；多选后的批量通过/拒绝收纳到“审核数据列表”标题行，不再额外占据一行。
  - 收紧待审核/已通过/已拒绝分组标签、搜索区和分页区的垂直间距；左侧工作区加宽并将对象 ID 与完整标题分两行展示。
  - 重新校准审核工作区高度，使固定底部操作栏和分页在首屏可见，减少底部无效留白，同时保留左右栏独立滚动。
  - 新增 `EditorField` 受控字段包装器，绕开 TDesign `FormItem` 对无字段名输入的默认值接管问题，修复编辑窗口出现空白或预填值被覆盖的问题。
  - 事实编辑窗口补齐多语言事实内容/时间说明、来源类型/来源/URL/原文、起止时间、关联实体/事件、矛盾/重复事实与审核状态。
  - 事件编辑窗口按新时间设计覆盖限时事件、周期事件、限时周期、时间未定；补齐多选分类、多语言别名/描述/时间说明、来源、备注，以及周期星期/时段/持续天数处理。
  - 实体编辑窗口补齐多选分类、多语言别名和描述、审核状态、是否作为分类节点；分类编辑窗口同步改为受控字段展示。
  - 事件/事实/实体列表保存回写补齐新字段，事件列表的“时间描述”改为读取真实 `timeDesc`，实体列表移除无业务意义的伪时间列并补充编辑入口。
- **影响文件**：
  - `src/cases/fact-db/components/{ReviewTab,FactDialog,EventDialog,EntityDialog,CategoryDialog,EditorField,FactTab,EventTab,EntityTab}.tsx`
  - `src/cases/fact-db/{review.css,style.css,review-mock.ts,types.ts}`
  - `需求细节变更记录.md`
- **验收点**：
  - 全选框位于条目 ID 左侧，多选操作不再占用独立行。
  - 待审核列表可显示对象 ID 与完整标题，类型/变更/优先级列保持可读。
  - 审核工作区底部操作栏和列表分页在首屏可见，页面底部无大面积无效留白。
  - 事实、事件、实体编辑窗口均显示预填数据、明确标签、完整字段分组，且事件可按时间类型切换对应字段。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理
- **验证说明**：
  - 已通过全新浏览器会话验证审核工作区：全选位于表头、选择两条数据后批量按钮收纳在标题行、固定底部操作与分页在首屏可见，浏览器控制台无错误。
  - 已分别打开事实、事件、实体编辑窗口，验证预填数据、多语言标签、分类、时间与来源字段正常渲染；事实窗口通过实际受控字段读取确认标题、分类和正文正确加载。
  - 相关 lint 通过，`npm run build` 全量通过。

---

### [2026-08-13 12:00] 审核详情交互重构：双视图、候选对比与泳道布局（内容审核）
- **需求原文**：
  - 原始视图按各类型编辑窗口字段全量展示，简化视图只展示存在差异的字段。
  - 冲突/重复/覆盖需要知道是跟哪条数据对比，提供对象标签；候选标签支持切换并自动调整差异高亮。
  - 原始视图改为左右两条纵向泳道：左侧标题是候选标签/线上版本，右侧标题是待审核新数据；新增无冲突时只展示右泳道并居中。
  - 原始字段按各类型编辑窗口展示，最后加来源信息片段（QA 原文等新数据生成依据）。
  - 历史任务联动：同事实多批覆盖时标记较旧任务为“已在较新任务中处理”。
  - 待审核列表展示冲突和重复数量标签，hover 显示候选对象 ID。
  - 类型进度标签“事实 0/1”，全部完成变浅绿底白字。
- **目标**：让审核人员能快速定位差异字段、清晰对比新旧对象，并减少任务列表信息噪音。
- **实现细节**：
  - 简化视图：无分区，按字段差异聚合成单一扁平列表；显示“差异 N 处”摘要；候选标签切换后差异高亮自动重算。
  - 原始视图：按事实/实体/事件编辑窗字段全量展示（标题、分类、事实内容、时间、来源、关联、矛盾/重复等），缺失字段以“（空）”占位；末尾统一追加“来源信息（新数据生成依据）”。
  - 候选标签栏：冲突红、重复橙、覆盖黄、新增绿；1 个候选静态展示、2 个并列、3+ 改下拉切换。
  - 左侧列表行：冲突/重复数量标签紧跟对象 ID，hover 展示候选对象 ID 与类型；类型列只保留类型本身。
  - 跨任务 superseded：`buildReviewTasks` 按 `factId` + 最新通过版本自动推算，历史任务中同事实在较新任务已通过时锁定重复审核并给出提示。
  - 新增无冲突类型：不展示差异/对比列，直接展示原文（右泳道居中）。
  - 类型进度标签：类型与数字之间加空格，全部完成时（pending=0）浅绿底白字。
  - 列头“优先级”完整展示；审核工作区左列比例 1:2。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review-types.ts`
  - `src/cases/fact-db/review-mock.ts`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 简化/原始视图切换正常；原始视图字段与编辑窗一致且左右泳道等高。
  - 候选标签可切换对比对象，差异高亮随候选变化。
  - 新增无冲突仅展示右泳道并居中。
  - 历史任务同事实已处理后显示“已在较新任务中处理”并锁定审核。
  - 类型进度标签含空格，完成态浅绿白字。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理
- **验证说明**：
  - 已通过本地浏览器验证简化扁平差异列表、原始两泳道、候选下拉、列表冲突/重复标签与 hover、类型进度完成态、superseded 锁定提示。
  - 相关 lint 通过，`npm run build` 全量通过。

---

### [2026-08-13 18:30] 简化/原始视图对比标签与并排展示（内容审核）
- **需求原文**：
  - 原始视图不保留说明文本。
  - 覆盖/重复/冲突对比要展示差异并对冲突段落高亮。
  - 简化视图对比标签与原始视图一致；≥2 个候选时不再下拉切换，改为下方依次并排 n 项对比。
  - 编辑新数据时自动切到原始视图。
- **目标**：减少原始视图的提示噪音，强化差异对比；让简化视图在多候选场景下一次性展示全部对比对象。
- **实现细节**：
  - 移除 `raw-config-mode-tip` 说明文本。
  - 原始视图新数据/线上数据列使用 `renderFieldDiff` 输出字符级差异（del/add 标记），覆盖/重复/冲突均高亮差异片段。
  - 简化视图按候选分组：1 个候选展示单组对比；≥2 个候选在 `.simple-diff-multi` 中依次渲染 `n` 张独立对比卡片，每张卡片包含一个候选的差异字段表，不再使用下拉切换。
  - 简化视图摘要行使用 `getCandidateTag` 输出 `重复/冲突/覆盖` 标签，与原始视图颜色一致（重复橙、冲突红、覆盖黄）。
  - 进入编辑态时通过 `useEffect` 监听 `editing`，自动调用 `onChangeViewMode("raw")` 切到原始视图。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 原始视图不再显示“原始视图按各类型编辑窗口…”说明。
  - 覆盖/重复/冲突字段均高亮差异片段（add 浅黄、del 浅红、相同部分不变色）。
  - 简化视图 ≥2 候选时呈现多张对比卡片，依次并排。
  - 点击“编辑新数据”自动切到原始视图。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理
- **验证说明**：
  - 已用 playwright-cli 进入 #10 任务实测 511 简化视图 2 个候选并排展示、字符级高亮、编辑按钮自动切到原始视图。
  - 相关 lint 通过，`npm run build` 全量通过。

---

### [2026-08-13 18:55] 简化摘要文案、新增视图模式、列表对象 ID 收敛（内容审核）
- **需求原文**：
  - 简化视图摘要行 `差异 对比 X · 新数据 · 共 n 个候选` 改为“待审核数据与线上相关数据（差异、冲突、重复）的差异对比一览信息”。
  - 新增无冲突时不再显示“简化/原始”切换，直接展示原始视图 UI。
  - 任务列表/详情对象 ID “新建·未分配” 改为 “未分配”，标题里去掉“（新建）”。
- **目标**：移除多余的解释文本，新增类型不再有视图切换；线上未分配 ID 统一为“未分配”。
- **实现细节**：
  - `simple-diff-summary` 摘要文案替换为统一描述，去除候选对比的 “对比 X · 新数据” 与 “共 n 个候选” 文本。
  - `diff-view-switch` 仅在 `!showSourceOnly`（非新增无冲突）时渲染，新增无冲突场景只展示右泳道。
  - `getObjectId` 把 `新建·未分配` 统一为 `未分配`；`getComparisonTargetLabel` 同步更新为 `事实 未分配`、`实体 未分配`、`事件 未分配` 形式。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `需求细节变更记录.md`
- **验收点**：
  - 简化视图仅显示 `差异 待审核数据与线上相关数据（差异、冲突、重复）的差异对比一览信息`。
  - 新增无冲突条目（如实体新建）不再显示简化/原始切换。
  - 列表行对象 ID 与详情头标题统一显示为 `未分配`。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理
- **验证说明**：
  - 已通过本地浏览器验证。
  - lint 通过，`npm run build` 全量通过。

---

### [2026-08-13 19:10] 原始视图泳道标题与高度对齐（内容审核）
- **需求原文**：
  - 左泳道标题改为“库内数据”左对齐；右泳道不展示标题文本，但展示对象 ID（覆盖显示对象 ID，新增显示 “- (暂无ID)”）。
  - 左右两侧标题行高度写死保持一致，避免标题行高度岔开。
  - 截图无需等待同意，验证后可保留或直接跳过。
- **目标**：泳道标题语义统一、左右高度一致、新增无冲突时新数据泳道无具体 ID 标识。
- **实现细节**：
  - `raw-config-swimlane-label` 由“对比对象”改为“库内数据”，`justify-content: flex-start` 让标题左对齐。
  - 右泳道标题 `strong` 内容按变更类型分支：`changeType === "new"` 显示 “- (暂无ID)”，其他显示 `getObjectId(item)`。
  - 泳道 header 高度写死 44px，`flex: 1` 让标题区撑满，meta 用 `margin-left: auto` 推到右侧；新增 `raw-config-swimlane-id-empty` 灰色弱化。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 原始视图左泳道标题为“库内数据”+ 候选标签 + 快照时间。
  - 原始视图右泳道标题为“新数据”+ 对象 ID（覆盖）/ “- (暂无ID)”（新增）。
  - 左右泳道标题行高度一致。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理
- **验证说明**：
  - 已通过本地浏览器实测 #9 任务 504 覆盖类、#10 任务 511 覆盖类，泳道标题与高度一致。
  - lint 通过，`npm run build` 全量通过。

---

### [2026-08-14 10:10] 覆盖类型过滤与泳道标题左对齐（内容审核）
- **需求原文**：
  - 覆盖类型不应把自身作为重复候选。
  - 原始视图顶格标题（`库内数据` / `新数据`）需完全靠左对齐。
- **目标**：避免覆盖条目被错误归为重复；让顶格标题视觉上与左侧列表保持一致。
- **实现细节**：
  - `candidates` useMemo 过滤掉与 `item.factId` 相同的候选项（从 candidate 快照 fact_id 字段或 label 解析）。
  - `.raw-config-swimlane-title` 显式声明 `flex-direction: row`、`text-align: left`、`justify-content: flex-start`，避免父级 `.raw-config-column > header div` 把 layout 强制为 column 导致 strong 居中。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 覆盖类型不再把自身 factId 作为重复候选。
  - 原始视图顶格标题完全左对齐，与左侧列表一致。
- **结果**：
  - [x] 完成
  - [ ] 部分完成
  - [ ] 待处理
- **验证说明**：
  - 已通过本地浏览器实测覆盖类条目，对象 ID `#10042` 不再作为重复候选，泳道标题左对齐。
  - lint 通过，`npm run build` 全量通过。

---

### [2026-08-14 11:00] 审核详情多语言切换与阿拉伯语 RTL（内容审核）
- **需求原文**：
  - 审核详情下方增加与模拟端一致的多语言切换；实体影响标题、别名、描述，事件影响名称、别名、时间描述，事实影响事实内容、时间描述。
  - 阿拉伯语所有字段需使用 RTL，并适用阿拉伯语标点。
- **实现细节**：
  - 依据接口文档使用 `zh` / `en` / `ar` / `tr` / `ru` / `zh-hk` 六种语言；审核详情标题下一行加入“多语言信息”及六等分切换按钮。
  - 扩展审核差异/候选快照数据结构，按语言读取新旧值；字段范围对齐接口命名：实体 `entity_name`（前台标题）、`alias`、`description`，事件 `event_name`、`alias`、`time_description`，事实 `fact_text`、`time_description`。
  - 原始/简化视图和编辑保存均使用当前语言值；补齐事实覆盖、事件新增和实体新增的多语言样例。
  - 阿拉伯语字段容器及编辑框统一加 `dir="rtl"`、`lang="ar"`、右对齐和 `unicode-bidi: plaintext`；英文逗号、分号、问号在阿拉伯语保存/展示时规范为 `،`、`؛`、`؟`，空值改为中性破折号，删除标记为 `محذوف`。
  - 核对 PUBG Mobile 当前接口数据：事实新增条目字段为 `fact_text`，待创建载荷包含 `fact_text_*`、来源、关联实体和矛盾信息；事实删除/覆盖仅下发 `fact_text`；实体新增字段为 `entity_name`，载荷含 `entity_name`、`description`、`parent_game_id`；当前任务样本未见事件条目。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review-types.ts`
  - `src/cases/fact-db/review-mock.ts`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 六种语言按钮位于“审核详情”下一行，切换后仅指定的对象字段变化。
  - 阿拉伯语字段展示、差异高亮、空值/删除标记及编辑框全部 RTL 且右对齐。
  - 多语言改动不影响现有中文审核与通过/拒绝流程。
- **结果**：[x] 完成
- **验证说明**：lint 通过，`npm run build` 全量通过；本地浏览器实测实体新增和事实覆盖的阿拉伯语字段均为 `dir=rtl`、`lang=ar`、右对齐。

---

### [2026-08-14 11:30] 审核结果整批应用、归档与撤销（内容审核）
- **需求原文**：整批任务的审核条目全部处理完后才能应用结果；应用后数据正式入库上线或放弃，任务归档且不可撤销。最后一条待审处理完成时提示是否立即应用；通过、拒绝在应用前均可撤销并返回待审核队列。
- **实现细节**：
  - 任务生命周期明确为“审核中 → 全部完成待应用 → 已归档”；审核进度仍以已处理/总数计算，但仅“待应用”状态在任务列表操作列展示“应用结果”。
  - 点击“应用结果”后需二次确认；确认后写入整批应用日志，通过条目正式入库上线、拒绝条目放弃，任务归档。归档任务仅保留只读查看入口，不能再次应用或撤销。
  - 单条审核或批量审核正好覆盖任务内最后的待审核条目时，弹出“是否立即应用审核结果？”提示；取消后保留为待应用，可回任务列表操作列执行。
  - 已通过和已拒绝条目在详情固定操作区显示“撤销审核”；撤销后清除单条结论和审核人信息，条目回到待审核队列。整批归档后该入口替换为不可撤销提示。
  - 任务详情头部同步显示“全部完成，待应用结果”或“已归档，不可撤销”。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review-types.ts`
  - `src/cases/fact-db/review-mock.ts`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 未完成审核的任务不展示“应用结果”；全部完成时显示且可执行确认。
  - 最后待审条目处理后可立即应用或稍后在任务列表应用。
  - 应用后归档不可撤销；应用前已通过/已拒绝均能撤销回待审核。
- **结果**：[x] 完成
- **验证说明**：lint 通过，`npm run build` 全量通过。

---

### [2026-08-14 12:00] 覆盖对象优先展示与审核新数据编辑（内容审核）
- **需求原文**：覆盖任务须优先展示“#10001 将被覆盖”；冲突、重复等其他问题收敛为下拉切换；双泳道标题单行左对齐；点击“编辑新数据”应激活可编辑字段。
- **实现细节**：
  - 更新类条目始终补齐并置顶覆盖候选；显式覆盖候选不再被自身事实 ID 过滤掉。
  - 用高优先级 CSS 覆盖通用列头规则：双泳道标题、对象 ID、覆盖标签和问题下拉固定为单行横向布局并左对齐。
  - 修复新增无冲突条目点击“编辑新数据”后未进入编辑态的问题；原始视图只对待审版本实际下发的字段显示编辑器；后端实际下发但未列入前台字段分区的字段会追加到原始视图并可编辑保存。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 覆盖条目优先显示“#对象ID 将被覆盖”；冲突/重复可切换查看。
  - 双泳道标题保持一行、左对齐。
  - 点击“编辑新数据”后，新数据实际下发字段出现可编辑输入框并可保存。
- **结果**：[x] 完成
- **验证说明**：lint 通过，`npm run build` 全量通过。

---

### [2026-08-14 12:20] 任务归档分组与审核进度筛选调整（内容审核）
- **需求原文**：未确认“应用结果”的任务仍属于可进入审核的任务；审核结论已全部完成时同时保留“进入审核”和“应用结果”两个操作；仅应用后归档。任务页签改为“待审核任务”“已归档任务”，有待审数据/已全部处理移入筛选。
- **实现细节**：
  - 任务页签改为“待审核任务”“已归档任务”：所有尚未应用结果的任务都保留在待审核任务；只有写入应用结果后才进入已归档任务。
  - 审核结论已全部完成但尚未应用时，操作列并排展示“进入审核”和“应用结果”。
  - 筛选区新增“审核进度”：全部审核进度、含待审核条目、已全部处理；不再以进度作为任务归档分组。
  - 已归档任务操作调整为“查看归档”，保持只读查看且不可撤销。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 未应用结果的已完成任务仍显示于“待审核任务”，并可进入审核。
  - 已完成待应用任务同时可“进入审核”“应用结果”。
  - 只有应用结果后任务才进入“已归档任务”；审核进度通过筛选查看。
- **结果**：[x] 完成
- **微调**：“应用结果”字体与“进入审核”一致（12px / 500），颜色改为绿色（`#1f9254`，悬停加深）。
- **验证说明**：lint 通过，`npm run build` 全量通过。

---

### [2026-08-14 13:30] 候选切换交互演变：+N 下拉 → 重叠露边 → 统一单一标签（内容审核）
- **需求原文（最终版）**：不再用“#10001 将被覆盖”形式，统一为“事实 #10001 覆盖”标签样式；顶层标签的类型（如重复）后加 `+n` 小圈（其余问题数量）；最右侧为下拉箭头；点击任何位置均为下拉切换；下拉列表保持候选原始顺序，仅切换选中高亮，不重排。
- **实现细节**：
  - `CandidateTabsBar` 收敛为：单候选静态标签；多候选统一走 `CandidateSelectBar` 单一选择器。
  - 触发器一个标签内依次为：`对象类型 #ID`（如“事实 #10001”）+ 类型小标签（覆盖灰 / 冲突红 / 重复橙）+ `+n` 计数圈 + `▾` 下拉箭头。
  - 下拉菜单按候选原始顺序渲染，仅切换 `is-active` 高亮，不重排被点击项。
  - 删除中间迭代的“+N个问题”独立按钮、重叠标签按钮与描边露边样式。
  - 简化视图多候选卡片与原始视图泳道标题均使用同一统一标签样式。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 候选仅一个标签，格式为“对象类型 #ID + 类型 +（+n）+ ▾”。
  - 点击标签任意位置打开下拉；选中项立即切换展示至最前。
  - 下拉列表保持候选原始顺序，仅切换高亮。
  - 覆盖对象默认非强调灰色，冲突红、重复橙。
- **结果**：[x] 完成
- **验证说明**：lint 通过，`npm run build` 全量通过。

---

### [2026-08-14 14:10] 多语言示例翻译优化（内容审核）
- **需求原文**：多语言示例当前版本有错别字/语义不清，请重新翻译为对应语言自然表达。
- **调整**：
  - 事实 501（极限对决模式上线）的 `fact_text` 多语言译文改为更自然的游戏公告口吻：en/Ar/tr/ru/zh-hk。
  - 事件 503（夏日狂欢）的 `description` 补充五种语言的自然翻译：en、Ar、tr、ru、zh-hk。
  - 实体 571（星火）的 `entity_name` 与 `alias` 在游戏本地化中通常保留专有名词音译，将 tr/ru 由意译改为统一音译 `Spark`；`description` 调整 en/Ar/tr/ru/zh-hk 为自然翻译。
- **影响文件**：
  - `src/cases/fact-db/review-mock.ts`
  - `需求细节变更记录.md`
- **验证说明**：lint 通过，`npm run build` 全量通过。
- **实现细节**：
  - `CandidateTabsBar` 收敛为：单候选静态标签；多候选统一走 `CandidateSelectBar` 单一选择器。
  - 触发器一个标签内依次为：`对象类型 #ID`（如“事实 #10001”）+ 类型小标签（覆盖灰 / 冲突红 / 重复橙）+ `+n` 计数圈 + `▾` 下拉箭头。
  - 下拉菜单列出全部候选（当前选中置顶高亮）；选择任一候选后触发器展示切换至该项，`+n` 更新为其余候选数量。
  - 删除中间迭代的“+N个问题”独立按钮、重叠标签按钮与描边露边样式。
  - 简化视图多候选卡片与原始视图泳道标题均使用同一统一标签样式。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 候选仅一个标签，格式为“对象类型 #ID + 类型 +（+n）+ ▾”。
  - 点击标签任意位置打开下拉；选中项立即切换展示至最前。
  - 覆盖对象默认非强调灰色，冲突红、重复橙。
- **结果**：[x] 完成
- **验证说明**：lint 通过，`npm run build` 全量通过。

---

### [2026-08-14 14:00] 回退修复：恢复 review-types 类型与变更记录（内容审核）
- **背景**：候选交互迭代过程中 `review-types.ts` 与变更记录被回退到旧版本，导致编译失败、历史记录缺失。
- **恢复内容**：
  - `review-types.ts`：恢复 `ReviewLanguage`/`REVIEW_LANGUAGE_OPTIONS`、`FieldDiff.translations`、`ReviewFieldSnapshot`、`ReviewTaskArchive`/`ReviewTaskApplicationStatus`/`applicationStatus`/`archive`、日志动作 `reverted`/`applied`。
  - `需求细节变更记录.md`：补回多语言 RTL、整批应用归档、覆盖优先与编辑、任务归档分组、统一标签等条目。
- **验证说明**：lint 通过，`npm run build` 全量通过。

---

### [2026-08-14 14:30] 待入库事实多语言补齐（内容审核）
- **需求原文**：导入与同步的待入库事实，事实文本和标题每种语言都要有自然翻译示例，便于审核员核对多语言切换效果。
- **调整**：
  - 事实 561（载具「沙暴」最高时速）：`fact_text` + `title` 补齐 en/Ar/tr/ru/zh-hk 五种语言翻译。
  - 事实 504（新英雄「星火」背景）：`fact_text` + `title` 补齐六语言翻译。
  - 事实 511（极限对决 S3）：`fact_text` + `time_description` + `title` 补齐六语言翻译。
  - 事实 551（空投刷新概率）：`fact_text` 补齐六语言翻译（含线上版与待审版）。
- **影响文件**：
  - `src/cases/fact-db/review-mock.ts`
  - `需求细节变更记录.md`
- **验收点**：
  - 切换多语言时，所有待入库事实的事实文本、标题、时间描述都能展示对应语言版本。
  - 覆盖版本（fact 551）的旧值与新值都按语言切换展示。
- **结果**：[x] 完成
- **验证说明**：lint 通过，`npm run build` 全量通过。

---

### [2026-08-14 15:30] 文档输出与甘特图回退（内容审核）
- **需求原文**：输出审核功能说明手册 HTML 与事件管理视觉说明 HTML；甘特图滚轮缩放改动不好用，回退为原逻辑。
- **实现细节**：
  - 工作区根目录新增《审核功能说明手册.html》：任务列表分组与筛选、详情双栏工作区、覆盖对象统一标签、多语言与 RTL、审核操作、整批应用与归档、字体制度。
  - 工作区根目录新增《事件管理视觉说明.html》：一级/二级视图结构、时间线甘特图结构与交互、周期事件月历、未分类表格、列表与树状视图、视觉规范。
  - `EventTab.tsx` 滚轮缩放逻辑恢复为修改前版本（轨道中段缩放、两侧滚动），提示文案同步恢复；lint 通过。
- **影响文件**：
  - `审核功能说明手册.html`（新增）
  - `事件管理视觉说明.html`（新增）
  - `src/cases/fact-db/components/EventTab.tsx`（回退）
  - `需求细节变更记录.md`
- **验证说明**：lint 通过。

---

### [2026-08-14 14:40] 左侧筛选不联动右侧当前数据（内容审核）
- **需求原文**：左侧列表添加筛选条件时，右侧当前数据展示保持不动，即使筛选结果里已不包含当前查看的数据。
- **实现细节**：
  - 新增 `lastDetailFiltersRef` 记录上一次详情筛选签名。
  - 详情自动跳转 effect 增加判断：当 `detailFilters` 变化（筛选签名不同）时跳过自动选中，保持 `activeItemId` / `activeVersionId` 不变；翻页、切换标签等场景仍按原逻辑跳转。
- **影响文件**：
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `需求细节变更记录.md`
- **验收点**：
  - 左侧加筛选后，右侧仍展示此前查看的数据。
  - 翻页或切换待审/已通过/已拒绝标签时，右侧正常切换。
- **结果**：[x] 完成
- **验证说明**：lint 通过，`npm run build` 全量通过。

---

### [2026-08-14 15:00] 审核页面字体制度统一（内容审核）
- **需求原文**：规范统一整体页面的字体、字号、字重、色号，以数据对比展示区为视觉基准，让整体看起来清爽干净。
- **字体制度（Typography Token）**：
  - 字号 4 档：标题 15px / 正文 13px / 辅助 12px / 微型 11px。
  - 字重 3 档：常规 400 / 中等 500 / 强调 600（消除 550、650 等非标准值）。
  - 文字灰阶 4 档：主 `#1f2328` / 次级 `#344054` / 辅助 `#8a9099` / 占位 `#c4c8ce`。
  - 字体族：中文 PingFang/微软雅黑，西文系统无衬线；编号/数字统一等宽（tabular-nums / mono）。
  - 状态色独立于灰阶：通过绿 `#15803d`、拒绝红 `#b91c1c`、待审橙 `#d97706`、强调蓝 `#4052c6`。
- **实现细节**：
  - 在 `.review-tab` 顶部声明字体制度变量（`--review-*`）并统一基础 font-family / 字号 / 主文字色。
  - 批量收敛文字色：主文字近似值（`#1f2329`、`#1d2129`、`#1d2939`、`#101828`）→ `#1f2328`；次级灰（`#475467`、`#4e5969`、`#5a6069`、`#596273`、`#53617a`、`#667085`）→ `#344054`；辅助灰（`#98a2b3`、`#9aa0a6`、`#7a8494`、`#7b8390`）→ `#8a9099`。
  - 微型标签 10px → 11px；游离 14px 数字 → 13px；字重 550/650 → 600。
  - 折叠箭头 16px 作为图标保留；状态语义色与差异高亮色保持不变。
- **影响文件**：
  - `src/cases/fact-db/review.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 页面只出现 15/13/12/11 四档字号，400/500/600 三档字重。
  - 文字灰阶仅 4 档，无近似灰混用。
  - 状态色与差异色保持原有语义不受影响。
- **结果**：[x] 完成
- **验证说明**：lint 通过，`npm run build` 全量通过。

---

### [2026-08-14 16:20] 批量删除入口 + 覆盖待审提示（实体/事件/事实管理，M2）
- **需求原文**：接口文档 v2.2 中全部能力本期落地（分 M1-M5 里程碑执行）；用户对 5 个决定点的回复：
  1. 归档/撤销端点后端方案 —— 暂缓，等用户反馈（不在本次范围）；且明确本期所有涉及"写入"的接口均只做只读 mock，不实现真实写入接口、不影响库内数据。
  2. 工具链落点 —— 临时页面，忽略，本期不实现。
  3. 检索页归属 —— 忽略，本期不做。
  4. 管理页批量审核状态恢复 —— 改为：若任务中某条数据存在覆盖的待审核版本，在标题后追加「有待审版本」标签，点击可跳转到内容审核并定位到该条目，不单独加列展示。
  5. 交付方式 —— 本次执行 M2（批量删除 + 覆盖待审提示），M1 待反馈，M4/M5 不做。
- **实现细节**：
  - 新增 `review-bridge.ts`：跨页导航桥。`navigateToReview(objectType, objectId)` 派发全局自定义事件 `factdb:navigate-review`；`navigateToReviewList()` 仅跳转任务列表；`buildPendingCoverIndex()` 基于 `mockReviewItems` 中 `changeType === "update" && status === "pending"` 的条目，按 `objectType` 汇总出存在待审覆盖版本的对象 ID 集合；`parseReviewObjectId` 解析 `"#12062"` 形式的 objectId。
  - 新增 `PendingCoverTag.tsx`：管理页标题后的橙色小标签组件，点击 `stopPropagation` 并调用 `navigateToReview`。
  - `App.tsx` 监听 `factdb:navigate-review` 事件，切到「内容库管理 > 内容审核」菜单并把 `ReviewLocator` 透传给 `FactDbPage`。
  - `Page.tsx` / `ReviewTab.tsx` 新增 `locator` prop；`ReviewTab` 内新增 `enterTaskWithFocus`，收到 locator 后按 `objectType + objectId` 匹配到具体审核条目所属任务，直接进入任务详情并聚焦该条目（跳过原有的记忆恢复逻辑）。
  - `review-mock.ts` 新增任务 `SYNC-20260702-1400`（内容同步-覆盖更新），包含实体 #12062、事件 #10889、事实 #37958 三条 `update` 类型的待审条目，用于三个管理页联动演示；同步补充任务序号映射。
  - `EntityTab.tsx` / `EventTab.tsx` / `FactTab.tsx`：
    - 各自通过 `buildPendingCoverIndex()` 取得对应类型的待审覆盖 ID 集合，在列表/时间线/周期视图等名称列后按需渲染 `PendingCoverTag`；统计栏新增「待审版本 N」可点击项，点击调用 `navigateToReviewList()`。
    - 表格新增复选框列（`row-select` multiple）与 `selectedKeys` 状态；选中记录时在搜索栏下方展开批量操作栏（含「批量删除」按钮，二次确认弹窗展示待删除记录标题，仅本地状态移除，不调用任何真实写接口）。
  - `style.css` 新增 `.pending-cover-tag`（橙色小标签样式）、`.stat-item--link`（统计栏可点击态）、`.batch-action-bar` / `.batch-action-divider`（批量操作栏）、`.fact-title-ellipsis`（事实标题省略，与标签共存）。
- **影响文件**：
  - `src/cases/fact-db/review-bridge.ts`（新增）
  - `src/cases/fact-db/components/PendingCoverTag.tsx`（新增）
  - `src/App.tsx`
  - `src/cases/fact-db/Page.tsx`
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review-mock.ts`
  - `src/cases/fact-db/components/EntityTab.tsx`
  - `src/cases/fact-db/components/EventTab.tsx`
  - `src/cases/fact-db/components/FactTab.tsx`
  - `src/cases/fact-db/style.css`
  - `需求细节变更记录.md`
- **约束**：本次及后续所有涉及后端写入能力的实现，均只做前端只读展示 / 本地 mock 状态变更，不接入真实写入接口，不影响任何库内真实数据。
- **验收点**：
  - 三个管理页对存在待审覆盖版本的记录，标题旁出现「有待审版本」标签，点击后精确跳转到内容审核对应任务详情并聚焦该条目。
  - 统计栏「待审版本 N」点击后跳转到内容审核任务列表。
  - 多选记录后搜索栏下方出现批量操作栏，批量删除需二次确认，确认后仅移除本地状态数据。
- **结果**：[x] 完成（M2）；M1 待用户反馈；M3（检索页）、M4/M5 本期不做。
- **验证说明**：lint 通过，`npm run build` 全量通过；浏览器验证实体 #12062 / 事件 #10889 / 事实 #37958 标签跳转均精确定位到对应任务详情条目，事实管理批量删除全流程（勾选→批量操作栏→二次确认→本地数据移除→统计栏刷新）验证通过。

---

### [2026-08-14 17:50] 操作栏三按钮 + 事实页布局按最新样式重构（M2 收尾）
- **需求原文**：
  1. 实体管理列表的操作栏当前只有「编辑」一个按钮，参照最新样式（图 1）改为「编辑 / 查看记录 / 删除」三按钮。
  2. 事实管理页因列过多（事实内容 380、关联实体 240、来源内容 260、矛盾/重复等冗余字段），导致水平滚动条过长、交互不友好。按最新样式（图 2）精简展示字段、统一列宽，展示不全的数据用 `…` 截断（不强制要求水平滚动）。
  3. 改完后继续做 M2。
- **实现细节**：
  - **实体管理 / 事件管理 / 事实管理 操作栏三按钮**：编辑（`handleEdit`）+ 查看记录（`handleOpenLog` → `OperationLogDialog`）+ 删除（`handleDelete`）；事件管理将历史文案「操作记录」同步改为「查看记录」，三处（默认视图卡片图例、未确定分类表、周期表）一致。
  - **事实管理布局重构（去左栏 + 顶部分类 chip）**：
    - 移除原本占据 220px 的左侧分类侧栏（`.factdb-fact-sider`），表格主区 `.factdb-fact-main` 由 ~570px 扩展到 ~806px，避免无谓压缩。
    - 分类筛选改为顶部 chip 行（`.factdb-cat-chips`），从 `categoryTree` 渲染一级 + 二级分类（`.factdb-cat-chip--sub` 缩进样式），点击切换 `category` 状态并高亮 `is-active`；保留「+ 管理分类」入口调用 `CategoryDialog`。
    - chip 行下方为原工具栏（ID 搜索 / 搜索事实 / 语言 / 线上状态 / 排序 / 含多语言列 / + 新建事实 / 批量导入 / 导出）。
  - **事实管理列精简与列宽调整**：
    - 列总宽 ≈ 1440px，移除「时间描述 / 矛盾事实ID / 重复事实」三列（重复/矛盾归内容审核工作台，时间描述可由起止时间推得），保留与最新样式对齐的核心列：事实ID / 标题 / 事实内容 / 线上状态 / 上传状态 / 关联实体ID / 来源类型 / 来源 / 来源URL / 来源内容 / 开始时间 / 结束时间 / 关联事项 / 操作。
    - 列宽重新分配为：复选框 40 / ID 80 / 标题 180 / 事实内容 220 / 线上状态 90 / 上传状态 100 / 关联实体ID 140 / 来源类型 90 / 来源 110 / 来源URL 120 / 来源内容 140 / 开始 130 / 结束 130 / 关联事项 120 / 操作 170。
    - 所有文字列加 `ellipsis: true`（hover Tooltip 显示完整内容），事实内容额外用 `-webkit-line-clamp: 2` 限制为两行 + 省略号（`.fact-content-clamp`）。
    - 关联实体ID / 关联事项改为「前两条 + 余 N」折叠（避免横向撑开列宽），hover Tooltip 显示完整列表。
    - 来源URL 改为可点击 `<a target="_blank">`；空值显示灰色占位符 `-`。
  - **CSS 调整**：
    - `.factdb-fact-main` 移除左 padding（去左栏后不再需要 16px 留白）。
    - 新增 `.factdb-cat-chips`（顶部 chip 行容器）、`.factdb-cat-chips-label`（前缀文字）、`.factdb-cat-chip`（圆角胶囊按钮 + hover/active 态）、`.factdb-cat-chip--sub`（二级缩进 + 浅色）、`.fact-content-clamp`（2 行 line-clamp）。
  - `.pending-cover-tag` / `.batch-action-bar` 等 M2 既有样式继续保留。
- **影响文件**：
  - `src/cases/fact-db/components/EntityTab.tsx`
  - `src/cases/fact-db/components/EventTab.tsx`
  - `src/cases/fact-db/components/FactTab.tsx`
  - `src/cases/fact-db/style.css`
  - `需求细节变更记录.md`
- **验收点**：
  - 三个管理页表格的操作列均渲染为「编辑 / 查看记录 / 删除」三按钮；点击「查看记录」弹窗同 `OperationLogDialog`。
  - 事实管理页：表格无水平滚动条（`scrollWidth === clientWidth`），事实内容列最多两行 + 省略号，超长列 hover Tooltip 显示完整内容。
  - 顶部分类 chip 切换正常（点击「任务相关」过滤结果，激活态高亮），原左栏 220px 区域已移除。
  - M2 既有能力（批量删除 / 待审版本标签 / 统计栏跳转 / 标签精确定位）依然正常。
- **结果**：[x] 完成。
- **验证说明**：lint 通过，`npm run build` 全量通过；浏览器实测事实表 `scrollWidth === clientWidth = 806`，无水平滚动条；事实 37958 行「有待审版本」标签正常显示且可跳转；勾选两条事实后批量操作栏「已选 2 条 / 变更状态：→ 已下线 / 批量删除 / 清空选择」出现并可正常触发二次确认。

---

### [2026-08-14 18:30] 字体制度全站统一 + 筛选栏/设计风格简约化（实体管理 → 错误表述检测）
- **需求原文**：把字体标准应用在从「实体管理」到「错误表述检测」的所有模块；统一所有模块的设计风格（简约），尤其是筛选栏。（事实模块的列宽/滚动条调试暂停，不再继续调。）
- **实现细节**：
  - **全局字体制度 token（`style.css` `:root`）**：新增 `--font-sans` / `--font-mono`（字体族）、`--fs-title/body/caption/mini`（15/13/12/11px）、`--fw-regular/medium/semibold`（400/500/600）、`--ink-1/2/3/4`（#1f2328/#344054/#8a9099/#c4c8ce 灰阶）、`--status-green/red/orange/blue`（#15803d/#b91c1c/#d97706/#4052c6）。
  - **覆盖 TDesign 变量**：`--td-text-color-primary/secondary/placeholder/tertiary` → 制度灰阶；`--td-success/warning/error` 及 hover/active → 简约低饱和状态色（error 由原 `#F44E4E` 降为 `#b91c1c`，success `#15803d`，warning `#d97706`）。品牌主色 `--td-brand-color: #5b7cff` 保留不变。
  - **统一 `--ref-*` 框架灰阶**到制度：`--ref-ink` 系列由 Tailwind 灰阶（#111827 等）映射为 #1f2328/#344054/#8a9099/#c4c8ce。
  - **`.app-shell`**：应用 `font-family: var(--font-sans)`、字号 13px、颜色 `--ink-1`。
  - **页面头部**：`.page-title` 20px→18px（600）；`.page-subtitle` 13px→12px（`--ink-3`）。
  - **统计栏 `.page-stats`**：13px→12px、颜色 `--ink-3`；数字 `.stat-num` 用 `--font-mono` 等宽 + 13px + 600 + `--ink-2`；分隔符 `--ink-4`。
  - **筛选栏 `.factdb-toolbar`**：统一控件（`.t-input/.t-select`）字号 13px、高度 32px、圆角 6px、占位符 `--ink-4`、复选框标签 13px，底部细线分隔。
  - **视图切换 Tab `.factdb-view-tabs`**：字号 14px→13px、颜色 `--ink-3`。
  - **区块标题**：新增 `.factdb-tab-content .t-card h3` 统一为 15px/600/`--ink-1`。
  - **`error-detect.css`**：`.ed-step-card-title` 18px→15px、`.ed-step-num` 14px→13px、`.ed-step-card-desc`/`.ed-section-subtitle`/`.ed-step-card-model-label` 统一 12px；移除 `.ed-step-card:hover` 阴影、圆角 10px→8px（简约）。
  - **内联样式微调**：`QaTab`（`fontWeight 700`→600）、`ErrorDetectTab`（检测结果标题 16→15、事实#id 700→600、正文 14→13）、`ExtractTab`（上传区提示 14→13）。
- **影响文件**：
  - `src/cases/fact-db/style.css`
  - `src/cases/fact-db/error-detect.css`
  - `src/cases/fact-db/components/QaTab.tsx`
  - `src/cases/fact-db/components/ErrorDetectTab.tsx`
  - `src/cases/fact-db/components/ExtractTab.tsx`
  - `需求细节变更记录.md`
- **验收点**：
  - 实体/事件/事实/事实提取/问题回复/错误表述检测各模块：页面标题 18px、区块标题 15px、正文 13px、统计栏/辅助 12px；筛选栏控件统一 13px/32px/6px。
  - 状态色全站统一为绿 #15803d / 红 #b91c1c / 橙 #d97706；文字灰阶 #1f2328/#344054/#8a9099/#c4c8ce。
- **结果**：[x] 完成。
- **验证说明**：lint 通过（仅遗留 review.css 一处 pre-existing 的 `line-clamp` vendorPrefix 警告），`npm run build` 全量通过；浏览器验证实体管理筛选栏 input 13px、统计栏 12px 颜色 #8a9099，问题回复 h3 15px/600，错误表述检测 step 标题 15px，`--td-error-color` 已生效为 #b91c1c、`--td-success-color` #15803d。

---

### [2026-08-17 10:40] 审核流程改为「直接生效」：移除应用结果 / 归档 / 撤销
- **需求原文**：群里讨论后确定——审一条通过就直接生效更合理，不等一批全部审完再统一应用。不需要「应用结果」步骤；审核通过即通过，因此没有「撤销」操作类型；「归档」也不需要。按此预设梳理版本差异，修改为审核直接生效流程，移除不再需要的功能点。
- **版本差异梳理（直接生效预设下的版本关系）**：
  1. 线上始终只有一个「生效版本」（`liveVersion`），即最后一次通过审核的版本；所有待审版本均以它为差异基准做新旧对比。
  2. 通过（overwrite/create/delete）→ 待审版本字段值立即写入线上，成为新的生效版本与新的对比基准。
  3. 拒绝 → 仅放弃该待审版本，线上生效版本保持不变。
  4. 跨任务作废：同 factId 在较新任务中已通过时，历史任务同对象待审条目标记 superseded，只读不重复审核（该逻辑原本即符合直接生效语义，保留）。
- **实现细节**：
  - **类型层（`review-types.ts`）**：
    - `ReviewTaskApplicationStatus`：`"reviewing" | "ready_to_apply" | "archived"` → `"reviewing" | "done"`。
    - 删除 `ReviewTaskArchive` 接口与 `ReviewTask.archive` 字段。
    - `ReviewLogEntry.action`：`"approved" | "rejected" | "reverted" | "applied"` → `"approved" | "rejected"`。
  - **任务聚合（`review-mock.ts` `buildReviewTasks`）**：删除 applied 日志与 archive 推导，`applicationStatus = pending === 0 ? "done" : "reviewing"`。
  - **主组件（`ReviewTab.tsx`）**：
    - 删除 `TaskTab`（active/archived）、`taskTab` state 与列表页 Tabs；改为计数条 `.review-task-tabs-bar`（审核任务 N / 进行中 N 橙 / 已全部生效 N 绿）。
    - 任务操作列：`ready_to_apply` 的「应用结果」与 `archived` 的「查看归档」均移除；统一为「进入审核」（reviewing）/「查看结果 + 已全部生效」标记（done）。
    - 删除 `isTaskArchived` / `applyTaskResults` / `confirmApplyTaskResults` / `revertReview` 四个函数及 `DialogPlugin` import；`applyReview` 不再检查归档，且**通过时同步把 pendingVersions[0] 的 newValue 写入 `liveVersion`**（快照时间更新为审核时间），实现「通过即生效」的可见版本更新。
    - `reviewSingle` 成功提示改为「已拒绝并放弃该版本，线上继续沿用原生效版本」/「通过并覆盖/新增/删除，已立即生效」；`batchReview` 提示「批量通过并生效」；两处均不再弹出「是否立即应用审核结果」。
    - 详情页头部：`ready_to_apply`「全部完成，待应用结果」与 `archived`「已归档，不可撤销」替换为 done 时的「全部审核完成，结果已生效」。
    - `DiffPanel`：移除 `taskArchived` / `onRevert` props 与「撤销审核」按钮；已处理条目固定区文案改为「该条目已通过并立即生效（通过并覆盖）｜时间｜审核人」+「审核结论已生效，不可撤销」/「该条目已拒绝，线上继续沿用原生效版本」。
    - 删除不再使用的 `STATUS_TAG` 常量。
  - **样式（`review.css`）**：新增 `.review-task-tabs-bar` / `.review-task-tabs-count--reviewing` / `--done`、`.review-task-done-tag`；删除 `.review-revert-action`、`.review-task-tabs`（两处）、`.review-task-apply-action`、`.review-task-archived`、`.review-detail-archived`；`.review-detail-ready` 颜色改绿色 #15803d。
  - **文档**：根目录 `审核功能说明手册.html` 升级 v1.1——任务列表改为计数条、操作列改为「进入审核 / 查看结果」、04.2 改为「审核直接生效」、05 由「结果应用与归档」重写为「版本流转与差异基准」。
- **影响文件**：
  - `src/cases/fact-db/review-types.ts`
  - `src/cases/fact-db/review-mock.ts`
  - `src/cases/fact-db/components/ReviewTab.tsx`
  - `src/cases/fact-db/review.css`
  - `审核功能说明手册.html`
  - `需求细节变更记录.md`
- **验收点**：
  - 列表页无「待审核任务 / 已归档任务」Tabs，无「应用结果 / 查看归档」按钮；计数条展示「审核任务 / 进行中 / 已全部生效」。
  - 审核通过后：条目立即进入「已通过」，固定区显示「已通过并立即生效」，右侧对比面板的「线上生效版本快照时间」同步更新为审核时间；无撤销入口。
  - 审核拒绝后：固定区显示「已拒绝，线上继续沿用原生效版本」。
  - 任务全部处理完后头部显示「全部审核完成，结果已生效」，列表操作列变为「查看结果 + 已全部生效」，计数条实时更新。
- **结果**：[x] 完成。
- **验证说明**：lint 通过（仅遗留 review.css `line-clamp` pre-existing 警告），`npm run build` 全量通过；浏览器实测：列表计数条「审核任务（11）进行中（9）已全部生效（2）」→ 审核任务 #11 通过 2 条 + 拒绝 1 条后变为「进行中（8）已全部生效（3）」，#11 操作列变为「查看结果已全部生效」，详情页头部显示「全部审核完成，结果已生效」，通过条目的线上版本快照时间由「2026-01-05 09:00」更新为审核时间「2026-8-17 10:28:54」，全程无「应用结果 / 归档 / 撤销」入口。

---

### [2026-08-17 11:20] 【已记录·未实现】撤销 + 版本快照 + 过期失效机制
- **状态**：⏳ 仅记录需求，本期不实现（场景较复杂，待后续排期）。
- **需求原文**：要撤销也行，但得记录快照——任务生成时记录一条线上版本的快照；如果这条还没审核，第二天线上又更新了一个版本，较早的待审数据直接过期失效，在数据列表中记录为「已失效」。
- **完整需求梳理**：
  1. **撤销能力（恢复）**：审核通过/拒绝后允许撤销结论，但撤销不是简单改回 pending，而是**回滚到任务生成时的线上版本快照**。因此任务生成时必须对该对象记录一份「线上生效版本快照」（含字段值、多语言值与快照时间）。
  2. **版本时效 / 过期失效**：
     - 待审条目携带任务生成时的线上快照作为差异基准。
     - 若该条目尚未审核，而线上又产生了更新的版本（如第二天另一次更新通过审核），则较早的待审数据直接过期，不再允许审核，在数据列表中记录为「已失效」（失效原因：线上已有更新版本 / 已在更新的任务中处理）。
     - 与现有 `superseded`（同 factId 在新任务中已通过 → 历史任务同对象条目只读）是同一机制的泛化：不只覆盖「审核通过」，任何新的线上版本更新都会让旧待审条目失效。
  3. **对「审核直接生效」的影响**：直接生效流程不变；新增的是「撤销回滚」与「过期失效」两个补充机制，均以任务生成时的快照为锚点。
- **关键设计问题（待定）**：
  - 快照粒度：按条目（对象）记录，还是按任务整批记录？
  - 撤销边界：任务内该对象后续若又被其他来源二次更新，回滚到任务快照是否会覆盖二次更新？（可能只允许「最近一次生效更新」撤销）
  - 「已失效」在列表与详情中的展示位置与可操作性（只读查看原因 / 是否可跳转最新版本）。
  - 过期判定时机：进入任务时比对，还是实时轮询/推送。
- **结果**：[ ] 未实现，已记录，待排期。
