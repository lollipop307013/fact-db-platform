# Gbot 事实库 — 旧 PRD vs 1 期实施 diff 速览

> 文档作用：对照旧 PRD（TAPD 1010153191132726482）与本期 1 期 PRD，快速看到改动点。
> 作者：yzhinan
> 日期：2026-05-07

---

## 一、整体策略变化

| 维度 | 旧 PRD | 1 期 |
|---|---|---|
| 提取工作台 | 模式一（短文本快捷）+ 模式二（长文本三阶段） | **统一一个工作台**：左侧粘贴 / 上传，右侧批次审核 |
| 缓冲池 | 隐含在三阶段里（步骤三事实校验） | **显式独立模块**："提取结果审核"（原"审核缓冲池"） |
| 协作模式 | 未明确 | **共享提取结果**，按提取人筛选 |
| 状态机 | 仅事实四态 | **事实四态 + 缓冲池条目三态 + 实体/事件二态**，全状态机校验 |
| 操作记录 | 仅提到"操作记录"按钮 | **按记录粒度的 logs[]**，11 种 action，编辑自动 diff 摘要 |
| 多语言 | 全模块覆盖 | **本期不做**，2 期补 |
| 文件导入 | 旧 PRD 提到导入文件，但未细化 | **本期落地**：PDF / Word / Excel / CSV 解析进缓冲池 |

---

## 二、模块级 diff

### 2.1 实体 / 事件管理

| 项 | 旧 PRD | 1 期 |
|---|---|---|
| 列表视图 | ✓ | ✓ 沿用 |
| 树状视图 | ✓ | ✓ 沿用，但**不重构** |
| 网状视图 | ✓ | **不做** |
| 编辑表单字段 | 多语言 + 别名 + 描述 | **字段对齐**：name / description / tags / status；多语言 2 期 |
| 批量导入 | CSV 模板 | ✓ 沿用 |
| 操作记录 | 提到 | **完整 logs[] 落地** |

### 2.2 事实管理

| 项 | 旧 PRD | 1 期 |
|---|---|---|
| 列表筛选 | 含语言 / 审核 / 上传 / 矛盾 | ✓ 沿用 |
| 上传状态 | 给了"待上传 / 需要更新 / 已上传"枚举 | **重新定义为"同步状态"**：success / pending / failed；不提供"立即上传"按钮，仅作异常监测 |
| 分类侧边栏 | 树状目录 | ✓ 沿用 |
| 多语言 Tab | 中 / En / Ar / Tr / Ru 等 | **本期不做**，仅中文 |
| 矛盾 / 重复检测 | 表格列展示 ID | ✓ 沿用，并在缓冲池里做了 Tag 化 + 对比 Dialog |

### 2.3 事实提取（核心变化最大）

| 项 | 旧 PRD | 1 期 |
|---|---|---|
| 模式一：短文本快捷 | 独立 Tab，结果原地展开 | **取消独立 Tab**，与多段提取合并为单一界面 |
| 模式二：长文本深度解析（三阶段） | 步骤一：解析长文本 / 步骤二：确认新实体与事件 / 步骤三：事实校验与入库 | **三阶段彻底简化**为：左侧提取（含模式选择）→ 右侧批次审核（含字段对齐+冲突对比+操作日志） |
| 文件导入 | 旧 PRD 提及但未拆细 | **本期落地**：含 PDF/Word/Excel/CSV 解析，进缓冲池 |
| 预处理规则矩阵 | 5 项多选（去口语 / 替换时效 / 标准化 / 关联识别 / 主动挖掘） | **本期不做** UI，由后端 LLM 内置；用户不直接控制 |
| 网格卡片 | 步骤二的实体卡片 + 单选"加入新建 / 无效" | **重构为缓冲池行内的 Tag 与抽屉表单**，更紧凑 |
| 交通灯（绿/黄/红） | 步骤三的状态分类 | **本期改为 Tag 形式**直接挂在条目上：冲突 Tag（红） / 重复 Tag（黄） |
| 比对卡片 | 双栏对比（来源片段 / AI 提取 / 库内冲突事实） | **本期独立 CompareDialog**：顶部详情 + 双栏对比 + 字符级 diff 高亮 |
| 编辑后重新校验 | 旧 PRD 未涉及 | **新增**：抽屉里实时校验，保存时自动 diff 字段写日志 |

### 2.4 问题回复

| 项 | 旧 PRD | 1 期 |
|---|---|---|
| 整体 | 完整描述 | **本期沿用 Demo，不改造** |
| 价值定位 | 参考实体 / 事件 / 事实展示 | 价值确认为"检测库内知识的覆盖度" |
| 多轮 | 未提 | **不做**，2 期评估 |

### 2.5 错误表述检测

| 项 | 旧 PRD | 1 期 |
|---|---|---|
| 整体 | 完整描述 | **本期沿用 Demo，不改造** |
| 价值定位 | 模糊 / 实体 / RAG 召回 | 价值确认为"批量修复库内错误表述" |
| 自动落库 | 未提 | **不做**，2 期补"修复任务 → 缓冲池审核"闭环 |

---

## 三、本期相对旧 PRD 的"加项"

这些不在旧 PRD，但本期落地：

1. **缓冲池共享 + 提取人字段**：之前默认私有，现在共享 + 可筛选
2. **状态机锁定（已审核 / 已拒绝）**：避免反复横跳，新增"撤回"动作
3. **冲突 / 重复 Tag 化 + 字符级 diff 对比**：原 PRD 仅在表格列展示
4. **缓冲池条目级操作日志（11 种 action）**：与事实库日志解耦
5. **同步状态字段（异常监测）**：旧 PRD"上传状态"语义重塑
6. **新发现实体 / 事件的 keep / discard 二元决策**：原 PRD 是单选"加入新建 / 无效"，本期统一概念
7. **编辑后重新校验冲突机制**：新增

---

## 四、本期相对旧 PRD 的"减项"

这些旧 PRD 有，本期不做（已划入 2/3 期）：

1. **多语言字段管理**（2 期）
2. **长文本三阶段深度解析工作台**（彻底简化，永久取消三阶段形态）
3. **网状视图实体管理**（2/3 期视使用情况）
4. **预处理规则多选矩阵**（不暴露给用户）
5. **测试 / 正式环境同步流转**（3 期）
6. **错误表述自动修复落库**（2 期）

---

## 五、参考

- 1 期完整 PRD：`PRD-1期-Gbot事实库管理.md`
- 2 期占位 PRD：`PRD-2期-Gbot事实库管理.md`
- 3 期占位 PRD：`PRD-3期-Gbot事实库管理.md`
- 旧 PRD：https://tapd.woa.com/tapd_fe/10153191/story/detail/1010153191132726482
