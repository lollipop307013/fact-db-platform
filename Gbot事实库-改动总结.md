# Gbot事实库 改动总结

## 一、事实提取 Tab（ExtractTab）

### 1. 矛盾事实查看详情
- 检测到矛盾时，点击"查看详情"可拉起**编辑事实弹窗**（FactDialog），预填充矛盾事实数据

### 2. 关联实体/事件标签样式
- 关联实体、新发现实体、关联事件、新发现事件的 Tag 统一改为**灰色**（`theme="default" variant="light"`）

### 3. 添加下拉选择
- 四个区域（关联实体、新发现实体、关联事件、新发现事件）点击添加时，提供 **Select 下拉菜单**搜索选择
- 已有实体/事件为 `filterable`，新发现实体/事件为 `filterable + creatable`

### 4. 保存按钮状态管理
- 保存后按钮置灰显示"已保存"
- 有任何修改（内容、实体、事件、时间）后按钮恢复可用，文案变为"保存并覆盖"

---

## 二、事实管理 Tab（FactTab）

### 1. 表格列调整
- **移除**上传状态列 → 后重新**添加**上传状态列（在事实内容列后）
- **关联实体列**：改为灰色 Tag 标签样式，格式 `[ID] 名称`，hover 气泡显示完整内容（逗号分隔），超3行出现纵向滚动条
- **关联事件列**：同上，灰色 Tag + hover 气泡 + 纵向滚动条
- **矛盾事实列**：按逗号分隔为多个红色 Tag，超3行纵向滚动条，hover 气泡显示完整内容
- **语义重复事实列**：按逗号分隔为多个黄色 Tag，超3行纵向滚动条，hover 气泡显示完整内容
- **开始/结束时间列**：宽度从 110→170，`whiteSpace: nowrap` 确保一行完整显示
- Mock 数据中关联实体/事件改为 `[ID] 名称` 格式

### 2. 工具栏
- 添加**排序方式**下拉（带 ↕ SwapIcon 图标 + "排序:" 标签）
  - 选项：开始时间（新到旧/旧到新）、创建时间（新到旧/旧到新）、ID（小到大/大到小）

### 3. Mock 数据
- 第一行事实填充了所有字段：开始时间、结束时间、时间描述、关联事件、矛盾事实、语义重复事实
- Fact 类型新增 `duplicate` 字段

---

## 三、事件管理 Tab（EventTab）

### 1. 表格列
- "起始时间"列名改为**"开始时间"**
- 移除上传状态列

### 2. 工具栏
- **删除**开始时间日期筛选项
- 添加**排序方式**下拉（同事实管理）

### 3. 树状视图
- "全部分类"改为**"全部事件"**
- 删除右侧"+ 添加分类"按钮

### 4. 类型与 Mock
- `GameEvent` 类型新增 `uploadStatus` 字段
- Mock 事件数据添加 uploadStatus

---

## 四、错误表述检测 Tab（ErrorDetectTab）

### 1. 两阶段流程
- 新增**"检测"按钮**（在"判断并修复"前）
- 点击检测 → Loading → 显示检测结果（统计信息4列：召回事实数/模糊匹配/实体匹配/RAG召回）
- 点击判断并修复 → Loading → 显示修复结果（统计信息3列：已检查事实数/存在错误的事实数/已修复事实数）

### 2. 使用 TDesign 组件
- **统计信息**：Card + Statistic 组件
- **错误分析**：Alert 组件（`theme="warning"`），去掉 📋 emoji
- **事实卡片**：改用 `qa-fact-card` 样式（灰色底 + 左边框），与问题回复模拟的参考事实视觉一致
- **修复分析结果**：Alert 组件（success/error），去掉 ✓/✗ 文字符号，使用 TDesign 自带图标
- **无错误标签**：使用 `CheckCircleFilledIcon` 图标替代文字勾号

### 3. 筛选功能
- "隐藏无错误事实" checkbox + 搜索框靠右对齐（使用 `header` prop）
- 移除"清除"按钮
- 事实 ID 行与内容之间始终保持 16px 间距

---

## 五、问题回复模拟 Tab（QaTab）

### 1. 参考实体
- 实体名称用**灰色 Tag** 展示
- 去掉"说明："前缀

### 2. 参考实体关系
- 两个实体名用灰色 Tag + `→` 箭头连接

### 3. 参考事实
- ID 格式改为 `事实 #37958`（加粗蓝色文字）
- Mock 数据 ID 改为真实 ID

### 4. 参考事件
- 从"暂无相关事件"改为展示事件卡片
- 使用 `qa-fact-card` 样式，格式：`事件 #ID` + 分类 + 名称 + 起止时间

---

## 六、弹窗组件

### 1. 实体弹窗（EntityDialog）
- "标签（可多选）" → **"分类（可多选）"**
- placeholder "搜索标签..." → **"搜索分类..."**

### 2. 事件弹窗（EventDialog）
- "事件标签（实体，可多选）" → **"事件分类（实体，可多选）"**
- placeholder "搜索事件标签..." → **"搜索事件分类..."**

### 3. 事实弹窗（FactDialog）
- 来源类型从 Input 改为 **Select 下拉菜单**（任务相关-勇士学院/新兵认证、官方公告、游戏内提取、社区投稿、人工录入）

---

## 七、全局改动

### 1. 所有 Select 支持搜索
- 全项目 15 个 Select 组件统一添加 `filterable` 属性

### 2. 样式
- `qa-fact-card` 底色从蓝色系改为**灰色系**（`bg-color-container-hover`）

### 3. 类型定义（types.ts）
- `Fact` 新增 `duplicate?: string`
- `GameEvent` 新增 `uploadStatus: "已上传" | "未上传"`
