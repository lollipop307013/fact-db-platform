# 【AIGC】Gbot 事实库管理模块 — 3 期 PRD（占位）

> 文档版本：v0.2（占位草稿，2026-05-07）
> 状态：**远期规划**，本文档列出 3 期候选范围，具体实施前需再次评审与裁剪。

---

## 0. 阅读说明

- 本期目标：**事实库的"运维化 / 平台化 / 度量化"**——从工具走向平台。
- 文档结构与首期保持一致。

---

## 一、项目背景与目标

### 1. 3 期出发点

首期解决"能用"，2 期解决"好用 + 协作"，3 期解决"可度量、可治理、可对外"：
- **环境管理**：测试环境与正式环境的事实分离 + 同步流程
- **可观测**：事实质量、AI 提取准确率、覆盖度、问答命中率有面板
- **可对外**：把事实库的能力开放出去（API / 知识图谱对接 / 第三方接入）

### 2. 3 期目标

| # | 目标 | 衡量 |
|---|---|---|
| 1 | 测试 / 正式环境分离 | 测试环境改动不影响线上；同步动作可审计 |
| 2 | 数据质量看板 | 事实库覆盖度、问答命中率、错误率有日级别监控 |
| 3 | 知识库 OpenAPI | 外部团队可通过 API 查询 / 订阅事实变更 |
| 4 | 异常告警机制 | 同步失败 / 大批量错误 / 异常操作 触发企微 / 短信告警 |

---

## 二、整体产品架构（差异部分）

```
新增/改造模块
├─ 环境切换器             ··· 顶部全局组件
├─ 数据质量看板           ··· 一级菜单
├─ OpenAPI / 接入管理     ··· 系统设置下
└─ 告警规则配置           ··· 系统设置下
```

---

## 三、需求详细拆解

---

## 需求 1：测试 / 正式环境管理【P0】

### 1.0 业务目标

让业务方可以在测试环境验证事实质量，确认无误后再同步到正式环境，避免直接污染线上。

### 1.1 环境切换

- 全局顶部增加环境切换器（测试 / 正式）
- 当前环境的可视化标识（颜色不同 + 角标）
- 切换需二次确认（避免误操作）

### 1.2 环境隔离

- 测试 / 正式环境数据物理隔离（不同库或不同表）
- 实体 / 事件 / 事实 三类数据均隔离
- 提取审核 / 错误表述检测 在两环境独立运行

### 1.3 环境同步

- 测试 → 正式：批量选择测试环境的"已审核"事实，发起"同步到正式"
- 正式 → 测试：从正式环境拉取一份快照到测试，便于做修改实验
- 同步动作记录在跨环境同步日志中（审计用）

### 1.4 环境切换的状态机扩展

事实状态机扩展：

```
测试: 待审核 → 已审核 → 已上线（测试）
                            ↓ 同步
正式: 待审核 → 已审核 → 已上线（正式）⇄ 已下线
```

### 1.5 与其它模块的关联

- 首期 Demo 已预留 env 字段（`test` / `prod`），3 期正式启用
- 操作日志中 action="同步"复用，detail 标明源/目标环境

---

## 需求 2：数据质量看板【P0】

### 2.0 业务目标

把事实库的"健康度"以指标形式呈现，让业务方知道哪些地方需要补、哪些地方有问题。

### 2.1 覆盖度指标

- 总事实数 / 已上线事实数 / 待审核事实数（按分类、按语言、按业务）
- "盲区"识别：基于问题回复的失败 case，找出现有事实库无法回答的问题分布
- 趋势图：日 / 周 / 月

### 2.2 质量指标

- 冲突事实数量 / 占比
- 重复事实数量 / 占比
- 同步失败率
- 错误表述检测发现 / 修复 / 待修复 数量
- 平均审核耗时

### 2.3 命中率指标

- 问答命中事实数 / 命中率
- 高频引用的事实 Top N
- 引用次数为 0 的事实（即"陈年僵尸"）

### 2.4 人员维度

- 各人员的提取量 / 审核量 / 编辑量
- 各人员的事实"上线率"（提取后多久能审核入库）

---

## 需求 3：OpenAPI / 接入管理【P1】

### 3.0 业务目标

把事实库做成一个可对外服务的能力，其他业务团队（不限于游戏）可以接入。

### 3.1 OpenAPI

- 提供 RESTful API：查询事实 / 实体 / 事件
- 提供 webhook：订阅事实变更（创建 / 更新 / 上线 / 下线）
- API Key 管理（在"系统设置"中配置）
- 限流与配额

### 3.2 接入方管理

- 注册接入方（业务名 / 联系人 / 用途）
- 管理 API Key（生成 / 撤销 / 限制）
- 按接入方做调用统计

---

## 需求 4：告警与通知【P1】

### 4.0 业务目标

把"运维监控"主动化——异常发生时及时通知到人。

### 4.1 告警规则

支持配置：
- 同步失败率 > N% 持续 X 分钟
- 单次批量提取失败 > N 条
- 单人单日操作量异常（防滥用）
- 冲突事实新增 > N 条

### 4.2 通知渠道

- 企微机器人
- 短信（仅 P0 告警）
- 邮件
- 站内消息

---

## 需求 5：长文本三阶段深度解析（重新评估）【P2】

### 5.0 业务目标

旧 PRD 的"长文本三阶段工作台"——首期被简化掉了，3 期评估是否需要补回。

如果 1/2 期沉淀下来发现：**简化后的提取审核流对超长文档（10w+ 字）力不从心**，那么 3 期重新引入分阶段处理。

否则：**永久取消**，不再讨论。

---

## 需求 6：网状视图（可视化图谱）【P2】

### 6.0 业务目标

把实体 / 事件 / 事实之间的关系网状化展示，便于发现"孤岛事实"或"密集集群"。

### 6.1 候选交互

- 节点：实体 / 事件 / 事实
- 边：关联关系（事实关联实体 = 实线；事实冲突 = 红线；事实重复 = 黄线）
- 操作：缩放 / 拖拽 / 单击节点查看详情 / 双击节点跳到详情页

### 6.2 实施前提

需先评估业务方真实使用场景与价值；如果"看着花哨实际没人用"，**永久搁置**。

---

## 四、本期不做（长尾）

- 事实库与外部知识图谱（如 ConceptNet、行业知识图谱）的对接
- 多模态事实（图 / 视频 / 音频）支持
- 事实库的版本快照与回滚（点状回滚到某天的全库状态）
- 用户个性化推荐（针对每个玩家不同的事实子集）

---

## 五、版本记录

| 版本 | 日期 | 变更 | 作者 |
|---|---|---|---|
| v0.2 | 2026-05-07 | 编号体系改为数字层级 1 / 1.1 | yzhinan |
| v0.1 | 2026-05-07 | 占位草稿 | yzhinan |
